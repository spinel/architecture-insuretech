apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
  namespace: default
  labels:
    app: scaletestapp
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: scaletestapp
